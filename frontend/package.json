{
  "name": "kenyawatch",
  "version": "1.0.0",
  "scripts": {
    "start": "node backend/server.js",
    "install-all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
